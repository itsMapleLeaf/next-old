@import 'variables'

transition()
  transition: 0.2s ease all

border-highlight()
  transition()

  border-bottom: 2px solid lighten(fg-color, 20%)

  &:focus
    border-bottom-color: lighten(fg-color, 40%)

inherit-text()
  color: inherit
  font: inherit
